/*
  Write a function that takes a string of one or more words, and returns the
  same string but with five or more letters words reversed. Strings passed in
  will consist of only letters and spaces. Spaces will be included only when
  more than one word is presented.

  Examples:
    spinWords("Hey fellow warriors"); => returns "Hey wollef sroirraw"
    spinWords("This is a test"); => "This is a test"
    spinWords("This is another test"); => "This is rehtona test"
*/

var spinWords = function(string) {
    var stringSize = string.length;
    var answer ="";
    for (var i = 0; i < stringSize; i++) {
        
    }
}
